<form method="post" action="{{ path('app_entrainment_delete', {'id': entrainment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entrainment.id) }}">
    <button class="inline-flex items-center justify-center px-5 py-3 text-sm font-medium rounded-md shadow-sm text-white bg-red-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 transition duration-300">Delete</button>
</form>
