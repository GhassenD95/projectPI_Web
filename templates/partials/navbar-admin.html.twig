
<aside class="bg-gray-900 text-white w-64 flex flex-col shadow-xl min-h-screen">
    <!-- Logo -->
    <div class="p-6">
        <h1 class="text-2xl font-light tracking-wider"><span class="text-orange-400">D</span>ASHBOARD<span class="font-bold text-indigo-400">.</span></h1>
    </div>

    <!-- Navigation -->
    <nav class="flex-1 pt-6">
        <div class="px-6 pb-5">
            <p class="text-orange-400 text-xs uppercase font-semibold tracking-wider">Main</p>
        </div>

        <a href="{{ path('app_admin') }}" class="flex items-center px-6 py-3 {{ app.request.attributes.get('_route') is same as('app_admin') ? "text-gray-100 bg-indigo-800 border-l-4 border-indigo-400" : "text-gray-400 hover:bg-gray-800 hover:text-white transition duration-200 ease-in-out" }}">
            <i class="fas fa-columns w-5 text-center text-indigo-400"></i>
            <span class="ml-3 font-medium">Dashboard</span>
        </a>


        <div class="px-6 py-5">
            <p class="text-orange-400 text-xs uppercase font-semibold tracking-wider">Management</p>
        </div>

        <a href="{{ path('app_admin_utilisateur') }}" class="flex items-center px-6 py-3 {{ app.request.attributes.get('_route') is same as('app_admin_utilisateur') ? 'text-gray-100 bg-indigo-800 border-l-4 border-indigo-400' : 'text-gray-400 hover:bg-gray-800 hover:text-white transition duration-200 ease-in-out' }}">
            <i class="fas fa-users w-5 text-center"></i>
            <span class="ml-3">Users</span>
        </a>

        </a>
        <a href="#" class="flex items-center px-6 py-3 text-gray-400 hover:bg-gray-800 hover:text-white transition duration-200 ease-in-out">
            <i class="fas fa-clipboard-list w-5 text-center"></i>
            <span class="ml-3">Teams</span>
        </a>
        <a href="#" class="flex items-center px-6 py-3 text-gray-400 hover:bg-gray-800 hover:text-white transition duration-200 ease-in-out">
            <i class="fas fa-calendar w-5 text-center"></i>
            <span class="ml-3">Calendar</span>
            <span class="ml-auto flex-shrink-0 h-5 w-5 rounded-full bg-indigo-500 text-white flex items-center justify-center text-xs">3</span>
        </a>

        <div class="px-6 py-5">
            <p class="text-orange-400 text-xs uppercase font-semibold tracking-wider">System</p>
        </div>

        <a href="#" class="flex items-center px-6 py-3 text-gray-400 hover:bg-gray-800 hover:text-white transition duration-200 ease-in-out">
            <i class="fas fa-cog w-5 text-center"></i>
            <span class="ml-3">Settings</span>
        </a>
        <a href="#" class="flex items-center px-6 py-3 text-gray-400 hover:bg-gray-800 hover:text-white transition duration-200 ease-in-out">
            <i class="fas fa-bell w-5 text-center"></i>
            <span class="ml-3">Notifications</span>
        </a>
    </nav>

    <!-- User -->
    <div class="p-4 border-t border-gray-800">
        <div class="flex items-center py-2">
            <div class="w-10 h-10 rounded-full bg-indigo-500 flex items-center justify-center text-white font-medium overflow-hidden">
                <img src="/api/placeholder/128/128" alt="User avatar" class="w-full h-full object-cover" />
            </div>
            <div class="ml-3">
                <p class="text-sm font-medium">Alex Morgan</p>
                <p class="text-xs text-gray-400">Administrator</p>
            </div>
            <div class="ml-auto">
                <button class="text-gray-400 hover:text-white">
                    <i class="fas fa-ellipsis-v"></i>
                </button>
            </div>
        </div>
    </div>
</aside>



{#   main content #}
